# Copilot Instructions

## Project Guidelines
- When migrating Azure resources for the Hospital-AI project, both appsettings.json (production) and appsettings.Development.json must be kept in sync with new resource endpoints, since App Service defaults to Production environment and loads appsettings.json.